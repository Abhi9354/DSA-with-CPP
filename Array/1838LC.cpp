#include <bits/stdc++.h>
using namespace std;

 int maxFrequency(vector<int>nums,int k ){
     int s = 1;
     while (k > 0) {


 }
int main(){
    vector<int>nums;
    nums.push_back(1);
    nums.push_back(2);
    nums.push_back(4);
    int k = 5;
    maxFrequency(nums,k);
}
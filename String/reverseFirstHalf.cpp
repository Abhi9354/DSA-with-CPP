#include <iostream>
#include <string>
#include <algorithm>
#include <math.h>
using namespace std;
int main()
{
   // string a = "abcdefghi";
   // int n = a.length();
   // cout<<n<<endl;
   // reverse(a.begin()+0,a.begin()+n/2);
   // cout<<a;
   // long long x = 1532348567236;

   // string s;
   // s = to_string(x);
   // int n = s.length();
   
   //  long long ans = 0;
   // for (int i = 0; i < n; i++)
   // {
   //    int digit = x % 10;
   //    ans = digit + ans * 10;
   //    x = x / 10;
   // }
   // cout << ans;
    int range = pow(2,31)-1;
    cout<<range;
}
